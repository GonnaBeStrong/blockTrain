<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sixoneseven.blocktrain.repo.mapper.FileMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.sixoneseven.blocktrain.repo.entity.File">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="path" property="path" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 根据ID查询文件地址 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT id,path
        FROM file
        WHERE id = #{id,jdbcType=BIGINT}
    </select>

    <!-- 插入文件地址 -->
    <insert id="insert" parameterType="com.sixoneseven.blocktrain.repo.entity.File" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO file (
            path
        ) VALUES (
                     #{path,jdbcType=VARCHAR}
                 )
    </insert>

    <!-- 更新用户 -->

</mapper>